line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "B", "UP"]

[lint.isort]
known-first-party = ["traceforge"]

[lint.per-file-ignores]
"src/traceforge/formats.py" = ["E501"]
